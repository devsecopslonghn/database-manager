import AppShell from '../../components/AppShell';

export default function AccessPage(){return <AppShell breadcrumb="Tenant / Administration / RBAC"><div className="page-heading"><div><p className="eyebrow">ACCESS CONTROL</p><h1>RBAC administration</h1><p className="muted">Roles are enforced by the API and may be bound at tenant, project, environment or target scope.</p></div><button className="button primary" type="button">Add user</button></div><section className="role-grid">{[['TENANT_ADMIN','Full tenant control','Targets, plans, approvals, rollback and access administration'],['OPERATOR','Operational execution','Sync, plan, execute and rollback within assigned scope'],['VIEWER','Read-only evidence','Dashboard, inventory, operations and audit visibility']].map(([role,title,detail])=><article className="panel" key={role}><p className="label-caps">{role}</p><h2>{title}</h2><p className="muted">{detail}</p><div className="permission-list"><span>target:view</span><span>migration:plan</span><span>migration:execute</span></div></article>)}</section><section className="panel empty-state"><h2>No local bindings loaded</h2><p className="muted">Keycloak identity is mapped to SchemaOps roles; scoped bindings will be exposed by the access API.</p></section></AppShell>;}
